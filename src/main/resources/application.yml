spring :
  datasource :
    url : jdbc:mysql://localhost:3306/lush_lane?createDatabaseIfNotExist=true
    username : root
    password : ${DB_PASSWORD}

  flyway:
    enabled: true
    baseline-on-migrate: true